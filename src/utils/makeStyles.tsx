import { makeStyles } from "@mui/material";


export const useStyles =  makeStyles(() =>({
    customContainer : {
      backgroundImage: 'url("/public/assets/images/bg-sidebar-desktop.svg")',
      backgroundSize : 'cover',
      backgroundPosition : 'center',
    
    }
  })) 